**deepin v25 安装AMD ROCm**

ROCm（Radeon Open Compute） 是 AMD 推出的开源 GPU 计算平台，用于在 AMD GPU 上进行 通用计算（GPGPU），地位相当于 NVIDIA 的 CUDA。

如果你手头没有 AMD 的 GPU，只有核显，也可以借助于 ROCm，助力 AI 推理。比如我手头的迷你主机使用的 AMD HX 365 CPU，就是核显